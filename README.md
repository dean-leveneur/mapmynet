# MapMyNet

Projet étudiant (INSA Lyon - 3IF).
Petit simulateur de routage réseau interactif développé en Python (Tkinter).

L'application permet de visualiser un graphe de routeurs, de tester différents algorithmes de parcours (BFS, DFS, Dijkstra, A*) et d'observer le comportement du réseau en cas de pannes simulées.

**Lancement :**
`ash
python main.py
`
"@

 = @"
# RunMail

Projet de stage (Run Hélicoptères).
Outil web interne permettant de lire les e-mails entrants, de générer une proposition de réponse via l'API OpenAI, et de l'enregistrer automatiquement comme brouillon.

**Lancement :**
`ash
pip install -r requirements.txt
python main.py
`
*(Nécessite de configurer ses identifiants email et sa clé API au premier lancement).*